# Database Configuration
# Required for production deployment - use Neon PostgreSQL
# Get your connection string from: https://console.neon.tech
# In Railway: this will be automatically set as DATABASE_URL if you link a Neon database
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Server Configuration
# PORT: In Railway, this is automatically set to the port your service runs on
# HOST: In Railway, bind to 0.0.0.0 to accept connections
# NODE_ENV: Set to 'production' for Railway deployment
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Session Configuration (optional)
SESSION_SECRET=your-secret-key-change-this-for-production

# Push Notifications (Web Push API) - REQUIRED for notifications feature
# Generate keys with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key-here
VAPID_PRIVATE_KEY=your-vapid-private-key-here
VAPID_SUBJECT=mailto:your-email@example.com

# Cloudflare R2 Configuration - REQUIRED for image storage
# Get your R2 settings from Cloudflare dashboard
CF_R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
CF_R2_ACCESS_KEY_ID=your-access-key-id
CF_R2_SECRET_ACCESS_KEY=your-secret-access-key
CF_R2_BUCKET_NAME=chat-images
CF_R2_PUBLIC_URL=https://your-public-domain.com/images
