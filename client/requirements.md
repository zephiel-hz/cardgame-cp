## Packages
canvas-confetti | For celebration effects when pulling a rare card
@types/canvas-confetti | TypeScript definitions for canvas-confetti

## Notes
WebSocket connection assumes /ws endpoint on the same host
API returns dates as strings in JSON, frontend needs to parse them for the countdown timers
Authentication is simplified (selecting predefined users) and state is managed in a React Context backed by localStorage.
