{
  "buildCommand": "npm run build",
  "env": {
    "NODE_ENV": "production"
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/:path((?!.*\\.).*)$",
      "destination": "/api/[...routes]"
    }
  ]
}
